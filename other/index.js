const websocket = require('../other/websocket');
const socketio = require('../other/socket');
const jsonrpc = require('../other/json-rpc');
const grpc = require('../other/grpc');
const rpc = require('../other/rpc');

module.exports = {
  websocket,
  socketio,
  jsonrpc,
  grpc,
  rpc,
}